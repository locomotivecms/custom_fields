env:
  - COVERAGE=true
language: ruby
rvm:
  - 2.1.7
  - 2.2.3
services: mongodb
notifications:
  email:
    - didier@nocoffee.fr
before_install:
  - gem install bundler
branches:
  except:
    - gh-pages
sudo: required
dist: precise
