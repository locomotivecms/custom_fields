env:
  - COVERAGE=true
language: ruby
rvm:
  - 2.4.3
services: mongodb
notifications:
  email:
    - didier@nocoffee.fr
before_install:
  - gem install bundler
branches:
  except:
    - gh-pages
sudo: required
